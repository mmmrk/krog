<%= form_for(@reviewer) do |f| %>
<% if @reviewer.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(@reviewer.errors.count, "error") %> prohibited this reviewer from being saved:</h2>

		<ul>
		<% @reviewer.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
		</ul>
	</div>
<% end %>

	<div class="field">
		<%= f.label :person %><br />
		<%= f.text_field :person %>
	</div>
	<div class="field">
		<%= f.label :review %><br />
		<%= f.text_field :review %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>