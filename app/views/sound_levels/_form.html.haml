= form_for(@sound_level) do |f|
	- if @sound_level.errors.any?
		#error_explanation
			%h2 #{ pluralize(@sound_level.errors.count, "error") } prohibited this sound_level from being saved:
			%ul
				- @sound_level.errors.full_messages.each do |msg|
					%li= msg

	.field
		= f.label :name
		%br
		= f.text_field :name

	.actions
		= f.submit